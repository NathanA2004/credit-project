export default function HomePage() {
  return (
    <main>
      <h1>Smart Credit Card Tracker</h1>
      <p>
        Recommendation API is live at <code>POST /api/recommend</code>. Dashboard UI is the next
        frontend step.
      </p>
    </main>
  );
}
